import { Time } from 'src/app/models/time.model';

export class Arrival {
  flightNumber?: string;
  from?: string;
  scheduledTime?: String;
  airline?: string;
  aircraft?: string;
  status?:string;
  actualTime?:String;
}
